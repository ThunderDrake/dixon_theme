// Generated by CoffeeScript 1.12.7
(function() {
  jQuery(function($) {
    var ThemeAttrFilter;
    ThemeAttrFilter = (function() {
      function ThemeAttrFilter($range) {
        var input, input_max, input_min, inputs, max, max_v, min, min_v, range, range_i, step;
        range = $($range);
        range_i = range.find('.range-slider');
        input = range_i.get(0);
        input_min = range.find('input[name=^"min_"]');
        input_max = range.find('input[name=^"max_"]');
        min = parseInt(input_min.data('min'));
        max = parseInt(input_max.data('max'));
        min_v = parseInt(input_min.val());
        max_v = parseInt(input_max.val());
        step = parseInt(range_i.data('step'));
        noUiSlider.create(input, {
          start: [min_v, max_v],
          connect: true,
          step: step,
          orientation: "horizontal",
          tooltips: false,
          range: {
            'min': min,
            'max': max
          }
        });
        inputs = [input_min, input_max];
        input.noUiSlider.on('update', function(values, index) {
          var v;
          v = values[index] + '';
          v = v.replace(/\s/g, "");
          return inputs[index].val(parseInt(v));
        });
        $(inputs).each(function(index, field) {
          return $(field).on('change', function() {
            var v;
            v = [null, null];
            v[index] = $(this).val();
            return input.noUiSlider.set(v);
          });
        });
      }

      return ThemeAttrFilter;

    })();
    $.fn.theme_attr_filter = function() {
      new ThemeAttrFilter(this);
      return this;
    };
    return $(document.body).ready(function() {
      return $('.attr-slider').each(function() {
        return $(this).theme_attr_filter();
      });
    });
  });

}).call(this);

//# sourceMappingURL=attr-slider.js.map
